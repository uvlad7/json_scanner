#!/usr/bin/env ruby

# frozen_string_literal: true

argv = %w[bundle install]

if RUBY_VERSION >= "3.4"
  argv.push("--gemfile", "Gemfile_ruby_3_4")
elsif RUBY_VERSION < "2.7"
  argv.push("--gemfile", RUBY_VERSION >= "2.5" ? "Gemfile_ruby_2_5" : "Gemfile_ruby_2_3")
end

exec(*argv)
